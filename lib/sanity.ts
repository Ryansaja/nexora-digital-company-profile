import { createClient } from "@sanity/client";export const sanityClient=createClient({projectId:process.env.NEXT_PUBLIC_SANITY_PROJECT_ID||"replace-me",dataset:process.env.NEXT_PUBLIC_SANITY_DATASET||"production",apiVersion:"2025-01-01",useCdn:true});export const latestPostsQuery=`*[_type == "post"] | order(publishedAt desc)[0...3]{title, slug, excerpt, category, publishedAt}`;
