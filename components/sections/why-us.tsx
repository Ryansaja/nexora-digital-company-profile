import { SectionHeading } from "@/components/section-heading";import { why } from "@/lib/data";export function WhyUs({dict}:{dict:any}){return <section className="section-padding bg-brand-navy text-white"><div className="container"><SectionHeading eyebrow={dict.sections.why} title={dict.sections.whyTitle} light/><div className="mt-14 grid gap-6 lg:grid-cols-3">{why.map(w=>{const Icon=w.icon;return <article key={w.title} className="glass rounded-[2rem] p-8"><Icon className="h-9 w-9 text-brand-gold"/><h3 className="mt-7 font-display text-2xl font-bold">{w.title}</h3><p className="mt-4 leading-7 text-white/70">{w.description}</p></article>})}</div></div></section>}
