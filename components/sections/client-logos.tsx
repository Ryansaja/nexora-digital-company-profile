import { logos } from "@/lib/data";export function ClientLogos({dict}:{dict:any}){const loop=[...logos,...logos];return <section className="overflow-hidden border-y bg-white py-12"><p className="mb-8 text-center text-sm font-bold uppercase tracking-[0.25em] text-muted-foreground">{dict.sections.clients}</p><div className="flex w-max animate-marquee gap-10">{loop.map((l,i)=><div key={`${l}-${i}`} className="grid h-20 w-44 place-items-center rounded-2xl border bg-brand-mist font-display text-xl font-bold tracking-widest text-brand-navy/55">{l}</div>)}</div></section>}
