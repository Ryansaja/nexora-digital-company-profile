"use client";import Image from "next/image";import { useMemo,useState } from "react";import { SectionHeading } from "@/components/section-heading";import { projects } from "@/lib/data";import { cn } from "@/lib/utils";export function Portfolio({dict}:{dict:any}){const cats=["All",...Array.from(new Set(projects.map(p=>p.category)))];const[active,setActive]=useState("All");const items=useMemo(()=>active==="All"?projects:projects.filter(p=>p.category===active),[active]);return <section id="work" className="section-padding bg-brand-mist"><div className="container"><SectionHeading eyebrow={dict.sections.portfolio} title={dict.sections.portfolioTitle}/><div className="mt-10 flex flex-wrap justify-center gap-3">{cats.map(c=><button key={c} onClick={()=>setActive(c)} className={cn("rounded-full border px-4 py-2 text-sm font-bold transition",active===c?"bg-brand-navy text-white":"bg-white text-brand-navy hover:border-brand-gold")}>{c}</button>)}</div><div className="mt-12 grid gap-6 md:grid-cols-2 lg:grid-cols-3">{items.map(p=><article key={p.title} className="group overflow-hidden rounded-[2rem] bg-white shadow-sm transition hover:-translate-y-2 hover:shadow-premium"><div className="relative h-64 overflow-hidden"><Image src={`https://images.unsplash.com/${p.image}?auto=format&fit=crop&w=1000&q=80`} alt={p.title} fill sizes="(min-width:1024px) 33vw, 100vw" className="object-cover transition duration-700 group-hover:scale-110"/></div><div className="p-6"><div className="flex items-center justify-between"><span className="rounded-full bg-secondary px-3 py-1 text-xs font-bold text-brand-blue">{p.category}</span><span className="font-bold text-brand-gold">{p.result}</span></div><h3 className="mt-4 font-display text-2xl font-bold text-brand-navy">{p.title}</h3></div></article>)}</div></div></section>}
