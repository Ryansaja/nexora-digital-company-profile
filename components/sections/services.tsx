"use client";import { motion } from "framer-motion";import { ArrowUpRight } from "lucide-react";import { SectionHeading } from "@/components/section-heading";import { Card } from "@/components/ui/card";import { services } from "@/lib/data";export function Services({dict}:{dict:any}){return <section id="services" className="section-padding bg-white"><div className="container"><SectionHeading eyebrow={dict.sections.services} title={dict.sections.servicesTitle}/><div className="mt-14 grid gap-6 md:grid-cols-2 lg:grid-cols-3">{services.map((s,i)=>{const Icon=s.icon;return <motion.div key={s.title} initial={{opacity:0,y:18}} whileInView={{opacity:1,y:0}} viewport={{once:true}} transition={{delay:i*.05}}><Card className="group h-full p-8 transition duration-500 hover:-translate-y-2 hover:border-brand-gold hover:shadow-premium"><div className="grid h-14 w-14 place-items-center rounded-2xl bg-brand-navy text-brand-gold"><Icon className="h-7 w-7"/></div><h3 className="mt-8 font-display text-2xl font-bold text-brand-navy">{s.title}</h3><p className="mt-4 leading-7 text-muted-foreground">{s.description}</p><a href="#contact" className="mt-6 inline-flex items-center gap-2 font-bold text-brand-blue">Learn More <ArrowUpRight className="h-4 w-4 transition group-hover:translate-x-1"/></a></Card></motion.div>})}</div></div></section>}
