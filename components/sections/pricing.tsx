import { CheckCircle2 } from "lucide-react";import { SectionHeading } from "@/components/section-heading";import { Button } from "@/components/ui/button";import { pricing } from "@/lib/data";import { cn } from "@/lib/utils";export function Pricing({dict}:{dict:any}){return <section id="pricing" className="section-padding bg-white"><div className="container"><SectionHeading eyebrow={dict.sections.pricing} title={dict.sections.pricingTitle}/><div className="mt-14 grid gap-6 lg:grid-cols-3">{pricing.map(p=><article key={p.name} className={cn("rounded-[2rem] border p-8",p.featured?"bg-brand-navy text-white shadow-premium":"bg-white")}><p className="font-display text-2xl font-bold">{p.name}</p><p className="mt-5 font-display text-5xl font-bold">{p.price}</p><p className={cn("mt-4 leading-7",p.featured?"text-white/70":"text-muted-foreground")}>{p.description}</p><ul className="mt-8 space-y-4">{p.features.map(f=><li key={f} className="flex gap-3"><CheckCircle2 className="h-5 w-5 shrink-0 text-brand-gold"/><span>{f}</span></li>)}</ul><Button asChild className="mt-8 w-full" variant={p.featured?"gold":"default"}><a href="#contact">Choose {p.name}</a></Button></article>)}</div></div></section>}
