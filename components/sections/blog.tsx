import { ArrowUpRight } from "lucide-react";import { SectionHeading } from "@/components/section-heading";import { posts } from "@/lib/data";export function Blog({dict}:{dict:any}){return <section id="insights" className="section-padding bg-brand-mist"><div className="container"><SectionHeading eyebrow={dict.sections.blog} title={dict.sections.blogTitle}/><div className="mt-14 grid gap-6 lg:grid-cols-3">{posts.map(p=><article key={p.title} className="rounded-[2rem] border bg-white p-7 transition hover:-translate-y-2 hover:shadow-premium"><p className="text-xs font-bold uppercase tracking-[0.25em] text-brand-gold">{p.category}</p><h3 className="mt-4 font-display text-2xl font-bold text-brand-navy">{p.title}</h3><p className="mt-4 leading-7 text-muted-foreground">{p.excerpt}</p><a href="#" className="mt-6 inline-flex items-center gap-2 font-bold text-brand-blue">Read insight <ArrowUpRight className="h-4 w-4"/></a></article>)}</div></div></section>}
