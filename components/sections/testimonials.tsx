"use client";import { useState } from "react";import { Quote } from "lucide-react";import { SectionHeading } from "@/components/section-heading";import { testimonials } from "@/lib/data";import { cn } from "@/lib/utils";export function Testimonials({dict}:{dict:any}){const[i,setI]=useState(0);const t=testimonials[i];return <section className="section-padding bg-brand-navy text-white"><div className="container"><SectionHeading eyebrow={dict.sections.testimonials} title="What leaders say after shipping with us." light/><div className="mx-auto mt-14 max-w-4xl rounded-[2rem] border border-white/15 bg-white/10 p-8 text-center backdrop-blur md:p-12"><Quote className="mx-auto h-10 w-10 text-brand-gold"/><p className="mt-8 font-display text-3xl font-bold leading-tight text-balance md:text-5xl">“{t.quote}”</p><div className="mt-8"><p className="font-bold text-brand-gold">{t.person}</p><p className="text-white/60">{t.role}, {t.company}</p></div><div className="mt-8 flex justify-center gap-3">{testimonials.map((_,idx)=><button aria-label={`Show testimonial ${idx+1}`} key={idx} onClick={()=>setI(idx)} className={cn("h-3 w-3 rounded-full",idx===i?"bg-brand-gold":"bg-white/30")}/>)}</div></div></div></section>}
